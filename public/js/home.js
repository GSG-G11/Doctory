/* eslint-disable no-undef */
window.addEventListener('load', () => {
  getFetch('/doctors', handleDoctors);
});

// window.addEventListener('load', () => {
//   getFetch('/doctors', handleDoctors);
// });